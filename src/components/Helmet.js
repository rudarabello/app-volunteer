import { Helmet } from 'react-helmet';

export default function Hel() {
    return (
        <div>
            <Helmet>
                <title>Volunteer</title>
            </Helmet>
        </div>
    );
}
